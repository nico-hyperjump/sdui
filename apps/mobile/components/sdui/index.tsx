/**
 * Re-exports the mobile component registry from the shared sdui-components package.
 * This preserves the `@/components/sdui` import path used by screen files.
 */
export { mobileRegistry } from "@workspace/sdui-components";
