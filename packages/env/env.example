# How to use this env.example:
# 1. If you make changes to this file, run `pnpm build` from this `packages/env` directory.
# 2. Copy this file to .env.
# 3. Run ./dev-bootstrap.sh from the root of the monorepo to create symlinks of the .env file in other apps and packages.
# IMPORTANT: Always add comment above the env variable explaining what the var is for and where to get its value.

# The database with connection pool
DB_POOLING_URL="postgres://root:root@localhost:5432/turborepo" #required

# The database without connection pool for Prisma migration
DB_URL_NON_POOLING="postgres://root:root@localhost:5432/turborepo" #required

# The base64 encoded certificate for the SSL connection.
DB_CERT_BASE_64=

# this is used by demo app 
NEXT_PUBLIC_DEFAULT_LANGUAGE=de #required

# the default timeout of the operation
NEXT_PUBLIC_DEFAULT_TIMEOUT_MS=10000 #number 
